<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.recipeapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!-- <activity android:name=".MainActivity"> -->
        <activity android:name=".Recipe.RecipeMain">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Recipe.RecipeSearch"
            android:label="@string/recipeapp"/>

        <activity android:name=".Recipe.RecipeAsync" />
        <activity android:name=".Recipe.RecipeEmptyActivity"
            android:label="Recipe Search App"/>
        <activity android:name=".Recipe.RecipeDetailFragment" />
        <!-- <activity android:name=".Recipe.RecipeMain" /> -->
    </application>

</manifest>